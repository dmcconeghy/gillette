import '../styles/Header.css'

function Logo() {
    return (
        <div className="Logo">
            {console.debug("The Logo component has rendered")}
            <h1>The React Store</h1>
        </div>
    )
}

export default Logo